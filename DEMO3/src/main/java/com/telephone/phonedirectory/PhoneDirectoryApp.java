package com.telephone.phonedirectory;

import javafx.application.Application;

/**
 * Main class for launching the Phone Directory application.
 */
public class PhoneDirectoryApp {

    /**
     * Main method that starts the Phone Directory application.
     *
     * @param args command line arguments
     */
    public static void main(String[] args) {
        Application.launch(PhoneDirectory.class, args);
    }
}
